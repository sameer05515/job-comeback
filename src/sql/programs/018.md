# Retrieve customers who placed more than 5 orders in a single month.

```sql

-- Retrieve customers who placed more than 5 orders in a single month.


CREATE TABLE orders (
  orderId INT PRIMARY KEY,
  customer VARCHAR(50),
  orderDate DATE,
  amount INT
);

INSERT INTO orders VALUES
(1, 'Clark', '2025-01-02', 200),
(2, 'Clark', '2025-01-05', 300),
(3, 'Clark', '2025-01-10', 250),
(4, 'Clark', '2025-01-12', 400),
(5, 'Clark', '2025-01-15', 350),
(6, 'Clark', '2025-01-20', 500),
(7, 'Ava',   '2025-01-05', 600),
(8, 'Ava',   '2025-02-07', 200),
(9, 'Champa', '2025-01-02', 200),
(10, 'Champa', '2025-01-05', 300),
(11, 'Champa', '2025-01-10', 250),
(12, 'Champa', '2025-01-12', 400),
(13, 'Champa', '2025-01-15', 350),
(14, 'Champa', '2025-01-20', 500);


select * from orders;

-- customers who placed more than 5 orders in a single month 👇

SELECT customer, 
       YEAR(orderDate) AS order_year,
       MONTH(orderDate) AS order_month,
       COUNT(*) AS order_count
FROM orders
GROUP BY customer, YEAR(orderDate), MONTH(orderDate)
HAVING COUNT(*) > 5;


```